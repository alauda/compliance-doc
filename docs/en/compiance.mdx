---
weight: 20
---

# Compliance Service

## Overview

Compliance Service provides comprehensive security compliance scanning capabilities for Kubernetes clusters and MicroOS systems. This guide demonstrates how to create scan profiles, monitor scan results, and export compliance reports.

## Prerequisites

- Alauda Container Platform version: >= v4.1.0
- Plugin version: >= v1.1.0

## Quick Scan

After the plugin is installed, you can quickly create scans using the following templates.

### K8S STIG Scan

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: ScanSuite
metadata:
  name: stig-scan
  namespace: compliance-system
spec:
  profiles:
    - stig-k8s-v2r3-api-server
    - stig-k8s-v2r3-control-plane
    - stig-k8s-v2r3-general
    - stig-k8s-v2r3-kubelet
```

### K8S CIS Scan

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: ScanSuite
metadata:
  name: cis-scan
  namespace: compliance-system
spec:
  profiles:
    - cis-k8s-controlplane
    - cis-k8s-etcd
    - cis-k8s-master
    - cis-k8s-node
    - cis-k8s-policies
```

### MicroOS STIG Scan

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: os-scan
  namespace: compliance-system
spec:
  profile: stig-os-microos
```

## Creating Scan Profiles

Scan profiles define the scope and configuration of compliance scanning operations. The Compliance Service supports both STIG (Security Technical Implementation Guide) profiles for Kubernetes and MicroOS environments.

### STIG Kubernetes Profiles

The following examples demonstrate how to create STIG compliance scan profiles for different Kubernetes components:

#### API Server Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-stig-k8s-v2r3-api-server
  namespace: compliance-system
  annotations:
    compliance.security.alauda.io/force-scan: "true"
    compliance.security.alauda.io/job-mode: "aggregated"
spec:
  nodeScopeStrategy: auto
  scanType: node
  targetNodeRoles:
    - control-plane
    - worker
  profile: stig-k8s-v2r3-api-server
  nodeSelector: {}
  maxHistoricalResults: 3
  schedule: "0 2 * * *"
```

#### Control Plane Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-stig-k8s-v2r3-control-plane
  namespace: compliance-system
spec:
  profile: stig-k8s-v2r3-control-plane
  nodeScopeStrategy: auto
  scanType: node
  targetNodeRoles:
    - control-plane
    - worker
```

#### General Kubernetes Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-stig-k8s-v2r3-general
  namespace: compliance-system
spec:
  nodeScopeStrategy: auto
  scanType: all
  targetNodeRoles:
    - control-plane
    - worker
  profile: stig-k8s-v2r3-general
```

#### Kubelet Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-stig-k8s-v2r3-kubelet
  namespace: compliance-system
spec:
  nodeScopeStrategy: auto
  scanType: node
  targetNodeRoles:
    - control-plane
    - worker
  profile: stig-k8s-v2r3-kubelet
```

### MicroOS Scan Profile

For MicroOS operating system compliance scanning:

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-stig-os-microos
  namespace: compliance-system
spec:
  profile: stig-os-microos
  scanType: node
```

### CIS Kubernetes Profiles

The Compliance Service also supports CIS (Center for Internet Security) benchmarks for Kubernetes. CIS scans use kube-bench as the scanning engine.

#### Control Plane Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-cis-k8s-controlplane
  namespace: compliance-system
spec:
  profile: cis-k8s-controlplane
  nodeScopeStrategy: auto
  scanType: node
  targetNodeRoles:
    - control-plane
```

#### Etcd Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-cis-k8s-etcd
  namespace: compliance-system
spec:
  profile: cis-k8s-etcd
  nodeScopeStrategy: auto
  scanType: node
  targetNodeRoles:
    - control-plane
```

#### Control Plane Node Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-cis-k8s-master
  namespace: compliance-system
spec:
  profile: cis-k8s-master
  nodeScopeStrategy: auto
  scanType: node
  targetNodeRoles:
    - control-plane
```

#### Worker Node Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-cis-k8s-node
  namespace: compliance-system
spec:
  profile: cis-k8s-node
  nodeScopeStrategy: auto
  scanType: node
  targetNodeRoles:
    - worker
```

#### Policies Scan Profile

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: Scan
metadata:
  name: scan-cis-k8s-policies
  namespace: compliance-system
spec:
  profile: cis-k8s-policies
  nodeScopeStrategy: auto
  scanType: node
```

## Scan Configuration Parameters

The following table describes the key configuration parameters available for scan profiles:

| Parameter | Type | Scope | Description |
|-----------|------|-------|-------------|
| `spec.scanType` | string | k8s,os | Supports `platform`, `node`, and `all`. In platform mode, the system only scans Kubernetes resources. In node mode, the system only scans node files.|
| `spec.nodeScopeStrategy` | string | k8s | Scanning node selection mode. Supports `auto` and `manual` modes. In auto mode, nodes are selected based on rule-specific scope definitions. In manual mode, all rules are executed on specified nodes. |
| `spec.targetNodeRoles` | array | k8s, os | Optional. Restricts node roles. Valid values include `control-plane` and `worker`. When configured, participates in node filtering and intersects with nodeScopeStrategy mode. |
| `spec.nodeSelector` | object | k8s, os | Optional. Uses node labels for selection. When configured, participates in node filtering and intersects with nodeScopeStrategy mode. |
| `spec.schedule` | string | k8s, os | Optional. Configures scheduled tasks using cron syntax. Example: `"0 2 * * *"` for daily execution at 2 AM. |
| `spec.maxHistoricalResults` | int | k8s, os | Optional. Number of historical results to retain (default: 5). Example: `2` to keep only the latest 2 results. |
| `spec.profiles` | array | ScanSuite | Optional. List of profiles to associate with a ScanSuite for batch scanning. |
| `spec.scanTemplate` | object | ScanSuite | Optional. Template configuration for child Scans in a ScanSuite. |

## Creating ScanSuite (Batch/Scheduled Scans)

ScanSuite is a resource for batch management of compliance scans. It allows you to associate multiple Profiles and schedule automated scans.

### Basic ScanSuite Example

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: ScanSuite
metadata:
  name: test-scan-suite
  namespace: compliance-system
spec:
  profiles:
    - stig-k8s-v2r3-api-server
    - stig-k8s-v2r3-control-plane
    - stig-k8s-v2r3-general
    - stig-k8s-v2r3-kubelet
  maxHistoricalResults: 3
  scanTemplate:
    scanType: node
    nodeScopeStrategy: auto
    targetNodeRoles:
      - control-plane
    nodeSelector:
      node-role.kubernetes.io/control-plane: ""
```

### Scheduled ScanSuite Example

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: ScanSuite
metadata:
  name: scheduled-scan-suite
  namespace: compliance-system
spec:
  profiles:
    - stig-k8s-v2r3-api-server
    - stig-k8s-v2r3-control-plane
  schedule: "0 2 * * *"  # Run daily at 2 AM
  maxHistoricalResults: 3
  scanTemplate:
    scanType: node
    nodeScopeStrategy: auto
```

### ScanSuite with Email Notification

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: ScanSuite
metadata:
  name: notify-scan-suite
  namespace: compliance-system
spec:
  profiles:
    - stig-k8s-v2r3-api-server
  maxHistoricalResults: 3
  scanTemplate:
    scanType: node
    nodeScopeStrategy: auto
  reportDelivery:
    email:
      enabled: true
      recipientsRef:
        name: compliance-mail-recipients
```

### Node Scheduling Strategy

The `nodeScopeStrategy` parameter controls how nodes are selected for scanning:

| Strategy | Description |
|----------|-------------|
| `auto` | Nodes are automatically selected based on the benchmark rule's `spec.nodeScope`. Kube-bench maps nodes by `benchmark.file.type`. No job is created if there is no match. |
| `manual` | Run on all matched nodes (ignores rule-specific `nodeScope`). All rules are executed on the specified nodes. |

Important Notes:
- CIS/STIG baselines have different scheduling rules, so they cannot be scanned together in a single ScanSuite
- OS/Node scanning requires `scanType: node`
- CIS only supports `scanType: node`; `platform` and `all` are not supported because kube-bench does not support rule-level node selection

## Scan Annotations

The following annotations can be used to control scan behavior:

| Annotation | Scope | Description |
|------------|-------|-------------|
| `compliance.security.alauda.io/force-scan: "true"` | k8s, os | Manually triggers immediate scan execution. Automatically resets to false after scan completion. |
| `compliance.security.alauda.io/job-mode: "aggregated"` | k8s | Enables aggregated scanning mode. All rules defined in the Profile are executed in a single Scan Job, with nodes scanned sequentially. This mode is only effective for Kubernetes scans. |

## Monitoring Scan Results

To check the status and results of your compliance scans:

```bash
# Check scan status and results
kubectl get scan <scan-name> -n compliance-system -o jsonpath='{.status.phase}{"\t"}{.status.result}'
```

Example output:
```
Done NON-COMPLIANT
```

## Exporting Compliance Reports

Compliance Service exports reports in ZIP format, which includes HTML, CSV, Excel files, and metadata.

### Export Current Scan Reports

#### Single Scan ZIP Export

Export a single Scan report in ZIP format:

```bash
# Export single Scan ZIP (includes HTML/CSV/Excel/metadata)
NS=compliance-system
SCAN=test-scan-stig-k8s-api-server

kubectl create --raw "/apis/reports.compliance.security.alauda.io/v1alpha1/scans/${NS}/${SCAN}/export/zip" -f - <<< '{}' > ${SCAN}.zip
```

The ZIP file contains:
- `report.html` - Multiple tabs: Overview/Rule Summary/each node
- `report.xls` - Multiple sheets
- `report.csv` - Segmented data
- `metadata.txt` - ScanID/filter criteria/statistics

#### ScanSuite ZIP Export

Export the latest ScanSuite report:

```bash
# Export ScanSuite latest ZIP (includes aggregated CSV/XLS, metadata, and each child Scan's HTML/metadata)
NS=compliance-system
SUITE=test-scan

kubectl create --raw "/apis/reports.compliance.security.alauda.io/v1alpha1/scansuites/${NS}/${SUITE}/export/zip" -f - <<< '{}' > ${SUITE}.zip
```

### Export Historical Reports

To export historical scan reports, you need to get the runID or scanID first:

#### Get ScanSuite History RunID

```bash
# Get ScanSuite history runID
kubectl get scansuite -n compliance-system test-scan -o yaml | grep runID

# Example output:
# runID: 20260106-034701-d30c73
# runID: 20260106-035650-004246
# runID: 20260106-043648-7d189d
```

#### Export Historical ScanSuite Report

```bash
# Export historical ScanSuite report by runID
NS=compliance-system
SUITE=test-scan
RunID=20260106-035650-004246

kubectl create --raw "/apis/reports.compliance.security.alauda.io/v1alpha1/scansuites/${NS}/${SUITE}/export/zip?runID=${RunID}" -f - <<< '{}' > ${SUITE}-${RunID}.zip
```

#### Get Scan History ScanID

```bash
# Get Scan history scanID
kubectl get scan -n compliance-system os-scan -o yaml | grep scanID

# Example output:
# scanID: 2a14e1b6-e4d1-4121-ac21-84b64c9c26ee
# scanID: 3323e4dc-f8de-4017-9e34-b10642e0d883
# scanID: be4637ee-8d06-4aaf-9f5b-ca315475599d
```

#### Export Historical Scan Report

```bash
# Export historical Scan report by scanID
NS=compliance-system
SCAN=os-scan
SCANID=be4637ee-8d06-4aaf-9f5b-ca315475599d

kubectl create --raw "/apis/reports.compliance.security.alauda.io/v1alpha1/scans/${NS}/${SCAN}/export/zip?scanID=${SCANID}" -f - <<< '{}' > ${SCAN}-${SCANID}.zip
```

### Export Reports Using CURL

You can also export reports using CURL with bearer token authentication:

```bash
# Export ScanSuite report with CURL
APISERVER="https://<platform-address>/kubernetes/<cluster-name>"
TOKEN="<Bearer_token>"
NS=compliance-system
SUITE=test-scan

curl -k -X POST \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{}' \
  --output "${SUITE}.zip" \
  "${APISERVER}/apis/reports.compliance.security.alauda.io/v1alpha1/scansuites/${NS}/${SUITE}/export/zip"
```

```bash
# Export Scan report with CURL
APISERVER="https://<platform-address>/kubernetes/<cluster-name>"
TOKEN="<Bearer_token>"
NS=compliance-system
SCAN=os-scan
SCANID=be4637ee-8d06-4aaf-9f5b-ca315475599d

curl -k -X POST \
  -H "Authorization: Bearer ${TOKEN}" \
  -H "Content-Type: application/json" \
  -d '{}' \
  --output "${SCAN}-${SCANID}.zip" \
  "${APISERVER}/apis/reports.compliance.security.alauda.io/v1alpha1/scans/${NS}/${SCAN}/export/zip?scanID=${SCANID}"
```

Note: 
- The `-d '{}'` is required (API expects a POST body), otherwise you may get 415/400 errors
- Use `--output <file>` to save the binary ZIP and avoid terminal noise

## Best Practices

1. **Scheduled Scanning**: Use the `schedule` parameter to automate regular compliance checks
2. **Resource Management**: Configure `maxHistoricalResults` to manage storage usage
3. **Node Targeting**: Use `targetNodeRoles` and `nodeSelector` to focus scans on specific infrastructure components
4. **Aggregated Mode**: Use aggregated job mode for large clusters to reduce Kubernetes API overhead
5. **Report Retention**: Regularly export and archive compliance reports for audit purposes

## Enable Report Email Delivery

Compliance Service supports sending scan reports via email. This feature requires the Alauda Container Platform Cluster Notification plugin to be installed in the workload cluster.

### Prerequisites

- Alauda Container Platform Cluster Notification plugin must be installed in the workload cluster

### Configure Notification Template

Create a NotificationTemplate resource to define the email content:

```yaml
apiVersion: ait.alauda.io/v1beta1
kind: NotificationTemplate
metadata:
  name: email-compliance-report-template
  namespace: cpaas-system
labels:
  cpaas.io/template.email.body.type: Html
  cpaas.io/template.language: EN
spec:
  templates:
    - type: Email
      subject: "Compliance {{ .labels.name }} Report"
      content: |-
        The compliance scan has completed. Please download the attached report ZIP file.
```

Note: 
- The resource name cannot be customized; Compliance sending reports relies on this name by default
- The notification template needs to be created in the `cpaas-system` namespace

### Configure Report Recipients

Create a ConfigMap to specify email recipients:

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: compliance-mail-recipients
  namespace: compliance-system
data:
  to: example1@alauda.io,example@gmail.com  # Recipient email addresses, separate multiple ones with commas.
```

Note: 
- The contact resource must be created under the `compliance-system` namespace
- The resource name must be consistent with the value of `reportDelivery.email.recipientsRef.name` in the Scan or ScanSuite resources

### Enable Email Notification in Scan/ScanSuite

Add the `reportDelivery` section to your Scan or ScanSuite configuration:

```yaml
apiVersion: compliance.security.alauda.io/v1alpha1
kind: ScanSuite
metadata:
  name: notify-scan-suite
  namespace: compliance-system
spec:
  profiles:
    - stig-k8s-v2r3-api-server
  maxHistoricalResults: 3
  scanTemplate:
    scanType: node
    nodeScopeStrategy: auto
  reportDelivery:
    email:
      enabled: true
      recipientsRef:
        name: compliance-mail-recipients
```
